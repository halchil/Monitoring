version: '3'
   
services:
  
  nginx:
    image: nginx:latest
    container_name: nginx
    ports:
      - "80:80"  # 外部アクセスをポート80で受け取る
    volumes:
      - ./nginx.conf/nginx.conf:/etc/nginx/conf.d/default.conf:rw  # カスタムNginx設定ファイル デフォルトで存在する nginx.confとdefault.confは書き換えない　もしかしたらderfault.confへの書き換えはOKかも
    restart: unless-stopped
    networks:  # ここでネットワークを指定
      - ecosystem

networks:
  ecosystem:
    external: true  # 既に作成されたネットワークを使用する場合